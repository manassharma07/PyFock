[pytest]
markers =
    slow: slower integral regression tests
    regression: complete SCF calculation regression tests
    long: larger full-calculation regression tests
    gpu: tests requiring a CUDA-capable GPU
